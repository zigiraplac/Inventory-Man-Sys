# Copy this file to .env and fill in real values. .env is gitignored —
# never commit real credentials.

# Password for the MySQL user the pipeline connects as (config/*.yaml's db_user).
DB_PASSWORD=changeme

# Used by docker-compose.yml to set the MySQL container's root password.
MYSQL_ROOT_PASSWORD=changeme
